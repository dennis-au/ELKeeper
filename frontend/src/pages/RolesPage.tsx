/** Compatibility route composition for the workload workspace. */
export {
  RolesWorkspace as RolesPage,
  managedWorkloadColumns,
  workloadImageVersion,
} from '../features/workloads/RolesWorkspace';
